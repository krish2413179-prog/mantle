{
  "name": "ghost-pay-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Ghost-Pay EIP-7702 gaming system",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network mantle-sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network mantle-mainnet",
    "verify": "hardhat verify --network mantle-sepolia",
    "test": "hardhat test",
    "generate-wallet": "node scripts/generate-agent-wallet.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "ethers": "^6.8.0"
  }
}
